import Dashboard from "@/components/dashboard";
export const metadata = {
  title: "Admin — Demonstração",
  robots: { index: false, follow: false },
};
export default function Admin() {
  return <Dashboard />;
}
